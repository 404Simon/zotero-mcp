[project]
name = "zotero-mcp"
version = "0.1.0"
description = "MCP server for read-only access to a Zotero library"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["fastmcp"]

[project.scripts]
zotero-mcp = "main:main"
